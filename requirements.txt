# Project 1 - AI-Powered Fake News Detection
# Core pipeline needs only the first three; scikit-learn is used solely for the
# verification section that cross-checks the from-scratch implementations.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
notebook>=7.0
